<% @map.each_with_index do |tiles, row| %>
  <div class="map_row <%= row %>" id="<%= @x %>,<%= @y %>">
    <% tiles.each do |tile| %>
        <div class="map_tile" id="<%= tile.x %>,<%= tile.y %>">
          <% if tile.town %>
              <% if tile.town.user_id == current_user.id %>
                <div class="town_user"></div>
              <% end %>
        <% end %>
        </div>
    <% end %>
  </div>
 <% end %>